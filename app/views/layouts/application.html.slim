doctype html
html
  head
    title famn
    = stylesheet_link_tag    'application', :media => 'all'
    = javascript_include_tag 'application'
    = csrf_meta_tags
    script type="text/javascript"
      | $(document).ready(       function() { $('bg_image').fullbg(); });
      | $(window).bind("resize", function() { $('bg_image').fullbg(); });
  body
    = image_tag('bg_image.jpg', :size => '640x480', :alt => '背景', :id => 'bg_image')
    div id="wrapper"
      div id="header" class="span12"
        div class="hero-unit"
          h1 famn
          p famnは、家族向けSNSです。家族の内輪でメッセージを共有します。
        = render '/shared/user_menu'
      div id='content'
        = yield
